name = "ado-tasks-mcp"
main = "src/worker.js"
compatibility_date = "2026-02-20"

# Set ADO_ORG and ADO_PROJECT via `wrangler.toml` vars or environment.
[vars]
ADO_ORG = ""
ADO_PROJECT = ""

# Optional: Add RATE_LIMIT_KV binding for persistent rate limiting storage
# [[kv_namespaces]]
# binding = "RATE_LIMIT_KV"
# id = "<your-kv-namespace-id>"
#
# If not configured, rate limiting falls back to SESSION_KV or in-memory storage
